# WARN: This terminal option settings should be NEVER removed it to avoid bash history overwrited
shopt -s histappend 

# Store a multi-line command line as a single line
shopt -s cmdhist # Enable by default


